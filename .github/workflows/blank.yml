name: CI

on:
  pull_request:
    types:
    - '*'
  push:
    branches:
    - master
    tags:
    - '*'


jobs:
  linux:
    strategy:
      matrix:
        pattern: [0,1,2]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      shell: bash
      run: |
        clang --version
        gcc --version
        cc --version
        echo ${{ matrix.pattern }}
        if [ ${{ matrix.pattern }} == 0 ]; then MY=1 && echo ${MY}; fi
        if [ ${{ matrix.pattern }} == 1 ]; then echo "one"; fi
        if [ ${{ matrix.pattern }} == 2 ]; then echo "two"; fi
  macos:
    strategy:
      matrix:
        pattern: [0,1,2]
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
  windows:
    strategy:
      matrix:
        pattern: [0,1,2]
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: test
      shell: powershell
      run: |
        #bitsadmin //TRANSFER htmlget "https://github.com/google/googletest/archive/release-1.7.0.zip" "${BASE}/googletest-release-1.7.0.zip"
        bitsadmin /TRANSFER htmlget https://github.com/google/googletest/archive/release-1.7.0.zip C:\temp\a.zip
        pwd
        mkdir build
        cd build
        # cmake -G "Visual Studio 16 2019" ..
        
                
